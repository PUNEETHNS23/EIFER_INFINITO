# Backend (FastAPI) dependencies
# Python 3.10+

fastapi>=0.100.0
uvicorn[standard]>=0.22.0
sqlalchemy>=1.4.0
pydantic>=2.0.0

# Auth (JWT + password hashing)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# passlib 1.7.x is not compatible with bcrypt 4+ (breaks on import/version detection)
bcrypt>=3.2.2,<4.0.0

# Needed for OAuth2PasswordRequestForm (form-data parsing)
python-multipart>=0.0.6

# PostgreSQL database adapter
psycopg2-binary>=2.9.0
